# Supervisor environment configuration

# Target process command to execute
SUPERVISOR_TARGET_CMD=uv run python -m simply_lumina

# Working directory for the supervisor to operate in
SUPERVISOR_WORKING_DIR=/path/to/simply-lumina

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
SUPERVISOR_LOG_LEVEL=INFO

# MCP server port for SSE (Server-Sent Events)
SUPERVISOR_MCP_PORT=5000
